kind: Service
apiVersion: v1
metadata:
  name: {{template "fullname" .}}
spec:
  ports:
  - port: 443
    targetPort: 443
    name: https
  - port: 80
    targetPort: 80
    name: http
  selector:
    app: {{template "fullname" .}}
  type: {{.Values.serviceType}}
